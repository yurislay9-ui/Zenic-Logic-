# ============================================================
#  TITAN OMNISCALE X - HTTP Server (HTTP-only mode)
#
#  This is the ACTIVE configuration for HTTP-only deployments.
#  When you obtain SSL certificates, replace this file with
#  the SSL configuration in titan-ssl.conf.example.
#
#  The HTTP→HTTPS redirect is intentionally NOT enabled here
#  because there is no HTTPS server block to redirect to.
#  Enable it only after setting up SSL (see titan-ssl.conf.example).
# ============================================================

server {
    listen 80 default_server;
    server_name _;

    # Let's Encrypt challenge (needed even in HTTP-only mode for cert generation)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API proxy
    location / {
        limit_req zone=api_limit burst=50 nodelay;

        proxy_pass http://titan_app;
        proxy_http_version 1.1;

        # WebSocket support for streaming
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Long timeouts for AI operations
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;

        # No buffering for streaming
        proxy_buffering off;
        proxy_cache off;
    }

    # Health and readiness (no rate limit)
    location ~ ^/(health|ready) {
        proxy_pass http://titan_app;
        proxy_set_header Host $host;
    }

    # Metrics (consider restricting)
    location /metrics {
        proxy_pass http://titan_app;
        proxy_set_header Host $host;
    }
}

# ============================================================
#  TITAN OMNISCALE X - HTTPS Server (SSL)
#  Uncomment and configure after obtaining SSL certificates:
#    docker compose run --rm certbot
#
#  IMPORTANT: When enabling SSL, you MUST also add an
#  HTTP→HTTPS redirect server block (see below) and REMOVE
#  the HTTP-only server block above to avoid two server_name _
#  blocks conflicting on port 80.
# ============================================================

# server {
#     listen 80;
#     server_name _;
#
#     # Let's Encrypt challenge
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     # Health check (available over HTTP for load balancers)
#     location /health {
#         proxy_pass http://titan_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     # Readiness check
#     location /ready {
#         proxy_pass http://titan_app;
#         proxy_set_header Host $host;
#     }
#
#     # Redirect everything else to HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl http2;
#     server_name _;
#
#     # ── SSL Configuration ──────────────────────────────────
#     ssl_certificate /etc/letsencrypt/live/${TITAN_DOMAIN}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/${TITAN_DOMAIN}/privkey.pem;
#
#     # Mozilla Modern TLS configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#
#     # OCSP Stapling
#     ssl_stapling on;
#     ssl_stapling_verify on;
#     resolver 8.8.8.8 8.8.4.4 valid=300s;
#     resolver_timeout 5s;
#
#     # HSTS (6 months — increase after confirming everything works)
#     add_header Strict-Transport-Security "max-age=15768000; includeSubDomains" always;
#
#     # ── Security Headers ───────────────────────────────────
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline';" always;
#
#     # ── API Proxy ──────────────────────────────────────────
#     location / {
#         limit_req zone=api_limit burst=50 nodelay;
#
#         proxy_pass http://titan_app;
#         proxy_http_version 1.1;
#
#         # Connection upgrade for WebSocket (streaming)
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Tenant-ID $http_x_tenant_id;
#
#         # Timeouts for long AI operations
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 120s;
#         proxy_read_timeout 120s;
#
#         # Buffering off for streaming responses
#         proxy_buffering off;
#         proxy_cache off;
#     }
#
#     # ── OpenAPI Docs (restrict to internal) ────────────────
#     location /docs {
#         proxy_pass http://titan_app;
#         proxy_set_header Host $host;
#         # Optional: restrict access
#         # allow 10.0.0.0/8;
#         # allow 172.16.0.0/12;
#         # deny all;
#     }
#
#     location /redoc {
#         proxy_pass http://titan_app;
#         proxy_set_header Host $host;
#     }
#
#     # ── Metrics (restrict to internal) ─────────────────────
#     location /metrics {
#         proxy_pass http://titan_app;
#         proxy_set_header Host $host;
#         # Restrict to internal networks only
#         # allow 10.0.0.0/8;
#         # allow 172.16.0.0/12;
#         # allow 127.0.0.1;
#         # deny all;
#     }
# }
