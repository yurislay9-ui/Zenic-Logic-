# Auto-generated from pyproject.toml — do not edit independently
# Source: pyproject.toml [project.dependencies] + [project.optional-dependencies]

# ============================================================
#  Core Dependencies  (pip install -e .)
# ============================================================

# Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0
jinja2>=3.1.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Database (SQLite — always available)
aiosqlite>=0.19.0

# AI/ML
numpy>=1.24.0

# Configuration
pyyaml>=6.0

# Production Server
gunicorn>=21.2.0

# Scheduling & Async
apscheduler>=3.10.0
aiohttp>=3.8.0
aiosmtplib>=3.0.0

# Auth & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0  # passlib 1.7.4 requires bcrypt <4.1 for __about__

# Utilities
aiofiles>=23.0.0

# ============================================================
#  Optional: PostgreSQL  (pip install -e ".[postgres]")
#  Not available on Termux/ARM — uncomment for Docker/production
# ============================================================

# asyncpg>=0.29.0
# psycopg2-binary>=2.9.9

# ============================================================
#  Optional: Other  (pip install -e ".[<group>]")
# ============================================================

# Z3 SMT Solver:  pip install -e ".[z3]"
# z3-solver>=4.12.0

# Textual TUI:  pip install -e ".[tui]"
# textual>=2.0.0

# Semantic Embeddings:  pip install -e ".[embeddings]"
# fastembed>=0.2.0

# Stripe Payments:  pip install -e ".[stripe]"
# stripe>=7.0.0

# Google Sheets:  pip install -e ".[sheets]"
# gspread>=5.0.0
# oauth2client>=4.1.0

# LLM (Qwen3-0.6B):  pip install -e ".[llm]"
# llama-cpp-python>=0.3.0

# PDF generation:  pip install -e ".[pdf]"
# weasyprint>=59.0

# ============================================================
#  Optional: Observability  (pip install -e ".[observability]")
# ============================================================

# aioredis>=2.0.0
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp-proto-grpc>=1.20.0
# prometheus-client>=0.19.0

# ============================================================
#  Testing Dependencies  (pip install -e ".[dev]")
#  Source: pyproject.toml [project.optional-dependencies.dev]
# ============================================================

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0
