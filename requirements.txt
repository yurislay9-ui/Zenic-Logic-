kivy==2.3.0
# Z3 SMT Solver (opcional - para verificacion formal completa)
# Si no se instala, el motor usa AC-3 + Backtracking como fallback automatico
# Instalar con: pip install z3-solver
# z3-solver>=4.12.0

# Mini AI Engine - Qwen3-0.6B Semantic Copilot (opcional)
# Si no se instala, el motor usa fallbacks determinísticos (TF-IDF + regex)
# El modelo GGUF debe estar en: models/qwen3-0.6b-q4_k_m.gguf
# Descargar: https://huggingface.co/unsloth/Qwen3-0.6B-GGUF
# llama-cpp-python>=0.3.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
