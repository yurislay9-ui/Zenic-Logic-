# Dependencias principales (compatibles con Android/Buildozer)
kivy==2.3.0
pydantic==2.7.1
pyyaml==6.0.1
numpy==1.26.4
httpx==0.27.0
aiofiles==23.2.1
python-constraint==1.4.0

# Dependencias solo para desktop/servidor (NO compatibles con Android ARM)
# Descomentar solo si se ejecuta en desktop con servidor HTTP:
# fastapi==0.111.0
# uvicorn==0.29.0
# fastembed==0.3.2
# tree-sitter-languages==1.10.2
