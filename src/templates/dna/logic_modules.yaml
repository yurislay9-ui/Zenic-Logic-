# =============================================================================
# Zenic Logic — Repository of Atomic Functions
# AI NEVER writes these from scratch; it assembles them.
# Descriptions in Spanish, code in English (Python / async / FastAPI-ready).
# =============================================================================

modules:

  # ===========================================================================
  # 1. SECURITY (5 modules)
  # ===========================================================================
  - id: "jwt_create"
    domain: "security"
    description: "Genera un token JWT firmado con claims personalizados y tiempo de expiración configurable"
    code_block: |
      import jwt
      from datetime import datetime, timedelta, timezone
      from typing import Any, Dict

      def jwt_create(payload: Dict[str, Any], secret: str, algorithm: str = "HS256", expires_minutes: int = 60) -> str:
          """Create a signed JWT token with expiration."""
          payload_copy = payload.copy()
          payload_copy["exp"] = datetime.now(timezone.utc) + timedelta(minutes=expires_minutes)
          payload_copy["iat"] = datetime.now(timezone.utc)
          token = jwt.encode(payload_copy, secret, algorithm=algorithm)
          return token
    dependencies: ["PyJWT"]
    verification_rule: "token_decodable"
    inputs: ["payload:Dict[str,Any]", "secret:str", "algorithm:str", "expires_minutes:int"]
    outputs: ["token:str"]

  - id: "jwt_verify"
    domain: "security"
    description: "Verifica y decodifica un token JWT, validando firma y expiración"
    code_block: |
      import jwt
      from typing import Any, Dict, Optional

      def jwt_verify(token: str, secret: str, algorithms: list = None, audience: Optional[str] = None) -> Dict[str, Any]:
          """Verify and decode a JWT token, raising on invalid/expired tokens."""
          if algorithms is None:
              algorithms = ["HS256"]
          decoded = jwt.decode(token, secret, algorithms=algorithms, audience=audience)
          return decoded
    dependencies: ["PyJWT"]
    verification_rule: "expired_token_raises"
    inputs: ["token:str", "secret:str", "algorithms:list", "audience:Optional[str]"]
    outputs: ["decoded:Dict[str,Any]"]

  - id: "password_hash"
    domain: "security"
    description: "Hashea una contraseña usando bcrypt con salt automático y verifica contra un hash existente"
    code_block: |
      import bcrypt

      def password_hash(password: str, rounds: int = 12) -> str:
          """Hash a password with bcrypt and auto-generated salt."""
          salt = bcrypt.gensalt(rounds=rounds)
          hashed = bcrypt.hashpw(password.encode("utf-8"), salt)
          return hashed.decode("utf-8")

      def password_verify(password: str, hashed: str) -> bool:
          """Verify a plaintext password against a bcrypt hash."""
          return bcrypt.checkpw(password.encode("utf-8"), hashed.encode("utf-8"))
    dependencies: ["bcrypt"]
    verification_rule: "hash_roundtrip"
    inputs: ["password:str", "rounds:int"]
    outputs: ["hashed:str"]

  - id: "api_key_validate"
    domain: "security"
    description: "Valida una clave API contra un almacén de claves, verificando prefijo, longitud, revocación y expiración"
    code_block: |
      import hashlib
      from datetime import datetime, timezone
      from typing import Dict, Optional

      def api_key_validate(api_key: str, key_store: Dict[str, Dict], prefix: str = "zk_") -> Dict[str, any]:
          """Validate an API key against a key store with prefix, revocation, and expiry checks."""
          if not api_key.startswith(prefix):
              return {"valid": False, "reason": "invalid_prefix"}
          key_hash = hashlib.sha256(api_key.encode()).hexdigest()
          record = key_store.get(key_hash)
          if record is None:
              return {"valid": False, "reason": "not_found"}
          if record.get("revoked", False):
              return {"valid": False, "reason": "revoked"}
          if record.get("expires_at") and datetime.now(timezone.utc) > record["expires_at"]:
              return {"valid": False, "reason": "expired"}
          return {"valid": True, "scopes": record.get("scopes", [])}
    dependencies: []
    verification_rule: "invalid_key_rejected"
    inputs: ["api_key:str", "key_store:Dict[str,Dict]", "prefix:str"]
    outputs: ["result:Dict[str,any]"]

  - id: "rbac_check"
    domain: "security"
    description: "Verifica si un usuario con roles dados tiene permiso para realizar una acción sobre un recurso según reglas RBAC"
    code_block: |
      from typing import Dict, List, Set

      def rbac_check(user_roles: List[str], action: str, resource: str, rbac_policy: Dict[str, Dict[str, Set[str]]]) -> bool:
          """Check if any of the user's roles permit the action on the resource."""
          for role in user_roles:
              role_permissions = rbac_policy.get(role, {})
              allowed_actions = role_permissions.get(resource, set())
              if action in allowed_actions or "*" in allowed_actions:
                  return True
          return False
    dependencies: []
    verification_rule: "unauthorized_denied"
    inputs: ["user_roles:List[str]", "action:str", "resource:str", "rbac_policy:Dict[str,Dict[str,Set[str]]]"]
    outputs: ["authorized:bool"]

  # ===========================================================================
  # 2. MESSAGING (4 modules)
  # ===========================================================================
  - id: "whatsapp_send"
    domain: "messaging"
    description: "Envía un mensaje de texto o plantilla a través de la API de WhatsApp Business"
    code_block: |
      import httpx
      from typing import Optional, Dict, Any

      async def whatsapp_send(phone_number: str, message: str, api_token: str, phone_number_id: str, template: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
          """Send a WhatsApp message via the Business API."""
          url = f"https://graph.facebook.com/v18.0/{phone_number_id}/messages"
          headers = {"Authorization": f"Bearer {api_token}", "Content-Type": "application/json"}
          payload = {"messaging_product": "whatsapp", "to": phone_number}
          if template:
              payload["type"] = "template"
              payload["template"] = template
          else:
              payload["type"] = "text"
              payload["text"] = {"body": message}
          async with httpx.AsyncClient() as client:
              resp = await client.post(url, json=payload, headers=headers, timeout=30.0)
              resp.raise_for_status()
              return resp.json()
    dependencies: ["httpx"]
    verification_rule: "message_id_returned"
    inputs: ["phone_number:str", "message:str", "api_token:str", "phone_number_id:str", "template:Optional[Dict[str,Any]]"]
    outputs: ["response:Dict[str,Any]"]

  - id: "email_send"
    domain: "messaging"
    description: "Envía un correo electrónico con soporte para HTML, archivos adjuntos y destinatarios en copia"
    code_block: |
      import asyncio
      import aiosmtplib
      from email.mime.multipart import MIMEMultipart
      from email.mime.text import MIMEText
      from email.mime.base import MIMEBase
      from email import encoders
      from typing import List, Optional

      async def email_send(to: List[str], subject: str, body: str, smtp_host: str, smtp_port: int, smtp_user: str, smtp_pass: str, html: bool = False, cc: Optional[List[str]] = None, attachments: Optional[List[bytes]] = None) -> str:
          """Send an email via SMTP with optional HTML and attachments."""
          msg = MIMEMultipart()
          msg["From"] = smtp_user
          msg["To"] = ", ".join(to)
          msg["Subject"] = subject
          if cc:
              msg["Cc"] = ", ".join(cc)
          msg.attach(MIMEText(body, "html" if html else "plain"))
          if attachments:
              for i, data in enumerate(attachments):
                  part = MIMEBase("application", "octet-stream")
                  part.set_payload(data)
                  encoders.encode_base64(part)
                  part.add_header("Content-Disposition", f"attachment; filename=attach_{i}")
                  msg.attach(part)
          result = await aiosmtplib.send(msg, hostname=smtp_host, port=smtp_port, username=smtp_user, password=smtp_pass, use_tls=True)
          return result[1] if isinstance(result, tuple) else str(result)
    dependencies: ["aiosmtplib"]
    verification_rule: "smtp_accepted"
    inputs: ["to:List[str]", "subject:str", "body:str", "smtp_host:str", "smtp_port:int", "smtp_user:str", "smtp_pass:str", "html:bool", "cc:Optional[List[str]]", "attachments:Optional[List[bytes]]"]
    outputs: ["status:str"]

  - id: "telegram_notify"
    domain: "messaging"
    description: "Envía una notificación a un chat de Telegram mediante un bot con soporte de parse_mode"
    code_block: |
      import httpx
      from typing import Optional, Dict, Any

      async def telegram_notify(bot_token: str, chat_id: str, message: str, parse_mode: Optional[str] = "HTML") -> Dict[str, Any]:
          """Send a notification message to a Telegram chat via bot."""
          url = f"https://api.telegram.org/bot{bot_token}/sendMessage"
          payload = {"chat_id": chat_id, "text": message}
          if parse_mode:
              payload["parse_mode"] = parse_mode
          async with httpx.AsyncClient() as client:
              resp = await client.post(url, json=payload, timeout=30.0)
              resp.raise_for_status()
              return resp.json()
    dependencies: ["httpx"]
    verification_rule: "ok_field_true"
    inputs: ["bot_token:str", "chat_id:str", "message:str", "parse_mode:Optional[str]"]
    outputs: ["response:Dict[str,Any]"]

  - id: "sms_send"
    domain: "messaging"
    description: "Envía un SMS a través de un proveedor como Twilio con validación de número de teléfono"
    code_block: |
      import httpx
      from typing import Dict, Any

      async def sms_send(to: str, body: str, account_sid: str, auth_token: str, from_number: str) -> Dict[str, Any]:
          """Send an SMS via Twilio REST API."""
          url = f"https://api.twilio.com/2010-04-01/Accounts/{account_sid}/Messages.json"
          payload = {"To": to, "From": from_number, "Body": body}
          async with httpx.AsyncClient() as client:
              resp = await client.post(url, data=payload, auth=(account_sid, auth_token), timeout=30.0)
              resp.raise_for_status()
              return resp.json()
    dependencies: ["httpx"]
    verification_rule: "sid_returned"
    inputs: ["to:str", "body:str", "account_sid:str", "auth_token:str", "from_number:str"]
    outputs: ["response:Dict[str,Any]"]

  # ===========================================================================
  # 3. PAYMENTS (4 modules)
  # ===========================================================================
  - id: "stripe_charge"
    domain: "payments"
    description: "Crea un cargo en Stripe usando un payment method o token con manejo de errores de tarjeta"
    code_block: |
      import stripe
      from typing import Dict, Any, Optional

      def stripe_charge(amount: int, currency: str, payment_method_id: str, customer_id: Optional[str] = None, metadata: Optional[Dict[str, str]] = None, stripe_api_key: str = "") -> Dict[str, Any]:
          """Create a Stripe payment intent and confirm it."""
          stripe.api_key = stripe_api_key
          params: Dict[str, Any] = {"amount": amount, "currency": currency, "payment_method": payment_method_id, "confirm": True, "automatic_payment_methods": {"enabled": True}}
          if customer_id:
              params["customer"] = customer_id
          if metadata:
              params["metadata"] = metadata
          intent = stripe.PaymentIntent.create(**params)
          return {"id": intent.id, "status": intent.status, "amount": intent.amount}
    dependencies: ["stripe"]
    verification_rule: "payment_succeeded"
    inputs: ["amount:int", "currency:str", "payment_method_id:str", "customer_id:Optional[str]", "metadata:Optional[Dict[str,str]]", "stripe_api_key:str"]
    outputs: ["result:Dict[str,Any]"]

  - id: "paypal_process"
    domain: "payments"
    description: "Procesa un pago a través de la API REST de PayPal con autenticación OAuth2"
    code_block: |
      import httpx
      from typing import Dict, Any

      async def paypal_process(order_id: str, client_id: str, client_secret: str, sandbox: bool = True) -> Dict[str, Any]:
          """Capture an approved PayPal order via REST API."""
          base_url = "https://api-m.sandbox.paypal.com" if sandbox else "https://api-m.paypal.com"
          async with httpx.AsyncClient() as client:
              token_resp = await client.post(f"{base_url}/v1/oauth2/token", data={"grant_type": "client_credentials"}, auth=(client_id, client_secret), timeout=30.0)
              token_resp.raise_for_status()
              access_token = token_resp.json()["access_token"]
              headers = {"Authorization": f"Bearer {access_token}", "Content-Type": "application/json"}
              capture_resp = await client.post(f"{base_url}/v2/checkout/orders/{order_id}/capture", headers=headers, timeout=30.0)
              capture_resp.raise_for_status()
              return capture_resp.json()
    dependencies: ["httpx"]
    verification_rule: "order_completed"
    inputs: ["order_id:str", "client_id:str", "client_secret:str", "sandbox:bool"]
    outputs: ["response:Dict[str,Any]"]

  - id: "refund_process"
    domain: "payments"
    description: "Procesa un reembolso total o parcial contra una transacción existente con razón opcional"
    code_block: |
      import stripe
      from typing import Dict, Any, Optional

      def refund_process(payment_intent_id: str, amount: Optional[int] = None, reason: Optional[str] = None, stripe_api_key: str = "") -> Dict[str, Any]:
          """Issue a full or partial refund against a Stripe PaymentIntent."""
          stripe.api_key = stripe_api_key
          params: Dict[str, Any] = {"payment_intent": payment_intent_id}
          if amount is not None:
              params["amount"] = amount
          if reason:
              params["reason"] = reason
          refund = stripe.Refund.create(**params)
          return {"id": refund.id, "status": refund.status, "amount": refund.amount}
    dependencies: ["stripe"]
    verification_rule: "refund_status_succeeded"
    inputs: ["payment_intent_id:str", "amount:Optional[int]", "reason:Optional[str]", "stripe_api_key:str"]
    outputs: ["result:Dict[str,Any]"]

  - id: "invoice_generate"
    domain: "payments"
    description: "Genera una factura en PDF con líneas de detalle, impuestos y totales calculados"
    code_block: |
      from datetime import datetime, timezone
      from typing import List, Dict, Any
      import uuid

      def invoice_generate(seller: Dict[str, str], buyer: Dict[str, str], line_items: List[Dict[str, Any]], tax_rate: float = 0.0, currency: str = "USD") -> Dict[str, Any]:
          """Generate an invoice data structure with computed totals."""
          subtotal = sum(item["quantity"] * item["unit_price"] for item in line_items)
          tax_amount = round(subtotal * tax_rate, 2)
          total = round(subtotal + tax_amount, 2)
          invoice = {"invoice_id": f"INV-{uuid.uuid4().hex[:8].upper()}", "created_at": datetime.now(timezone.utc).isoformat(), "seller": seller, "buyer": buyer, "line_items": line_items, "subtotal": subtotal, "tax_rate": tax_rate, "tax_amount": tax_amount, "total": total, "currency": currency, "status": "pending"}
          return invoice
    dependencies: []
    verification_rule: "totals_consistent"
    inputs: ["seller:Dict[str,str]", "buyer:Dict[str,str]", "line_items:List[Dict[str,Any]]", "tax_rate:float", "currency:str"]
    outputs: ["invoice:Dict[str,Any]"]

  # ===========================================================================
  # 4. SCHEDULING (4 modules)
  # ===========================================================================
  - id: "appointment_create"
    domain: "scheduling"
    description: "Crea una cita verificando disponibilidad del recurso y duración en minutos"
    code_block: |
      from datetime import datetime, timezone, timedelta
      from typing import Dict, Any, Optional

      def appointment_create(resource_id: str, user_id: str, start_time: datetime, duration_minutes: int, existing_bookings: list, metadata: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
          """Create an appointment after checking basic availability."""
          end_time = start_time + timedelta(minutes=duration_minutes)
          for booking in existing_bookings:
              b_start = booking["start_time"]
              b_end = booking["end_time"]
              if booking["resource_id"] == resource_id and start_time < b_end and end_time > b_start:
                  return {"success": False, "reason": "time_conflict", "conflict_with": booking.get("id")}
          import uuid
          appointment = {"id": f"apt-{uuid.uuid4().hex[:8]}", "resource_id": resource_id, "user_id": user_id, "start_time": start_time.isoformat(), "end_time": end_time.isoformat(), "duration_minutes": duration_minutes, "status": "confirmed", "metadata": metadata or {}}
          return {"success": True, "appointment": appointment}
    dependencies: []
    verification_rule: "conflict_rejected"
    inputs: ["resource_id:str", "user_id:str", "start_time:datetime", "duration_minutes:int", "existing_bookings:list", "metadata:Optional[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "calendar_collision_check"
    domain: "scheduling"
    description: "Detecta colisiones entre un rango de tiempo propuesto y eventos existentes en un calendario"
    code_block: |
      from datetime import datetime
      from typing import List, Dict, Any

      def calendar_collision_check(proposed_start: datetime, proposed_end: datetime, events: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
          """Return all events that collide with the proposed time range."""
          collisions = []
          for event in events:
              ev_start = event["start_time"] if isinstance(event["start_time"], datetime) else datetime.fromisoformat(event["start_time"])
              ev_end = event["end_time"] if isinstance(event["end_time"], datetime) else datetime.fromisoformat(event["end_time"])
              if proposed_start < ev_end and proposed_end > ev_start:
                  collisions.append(event)
          return collisions
    dependencies: []
    verification_rule: "no_false_positives"
    inputs: ["proposed_start:datetime", "proposed_end:datetime", "events:List[Dict[str,Any]]"]
    outputs: ["collisions:List[Dict[str,Any]]"]

  - id: "reminder_schedule"
    domain: "scheduling"
    description: "Programa recordatorios antes de una cita usando offsets de tiempo personalizables"
    code_block: |
      from datetime import datetime, timedelta
      from typing import List, Dict, Any

      def reminder_schedule(appointment_time: datetime, offsets_minutes: List[int], channel: str = "email", user_id: str = "") -> List[Dict[str, Any]]:
          """Generate reminder scheduled times from an appointment and offset list."""
          reminders = []
          for offset in offsets_minutes:
              reminder_time = appointment_time - timedelta(minutes=offset)
              reminders.append({"user_id": user_id, "appointment_time": appointment_time.isoformat(), "reminder_time": reminder_time.isoformat(), "offset_minutes": offset, "channel": channel, "status": "scheduled"})
          return reminders
    dependencies: []
    verification_rule: "reminders_before_appointment"
    inputs: ["appointment_time:datetime", "offsets_minutes:List[int]", "channel:str", "user_id:str"]
    outputs: ["reminders:List[Dict[str,Any]]"]

  - id: "cancellation_policy"
    domain: "scheduling"
    description: "Evalúa la política de cancelación y calcula penalización según antelación de la cancelación"
    code_block: |
      from datetime import datetime, timedelta
      from typing import Dict, Any, Optional

      def cancellation_policy(appointment_time: datetime, cancellation_time: datetime, policy_rules: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
          """Evaluate cancellation policy and compute penalty based on lead time."""
          if policy_rules is None:
              policy_rules = {"free_cutoff_hours": 24, "penalty_rate": 0.5, "no_show_rate": 1.0}
          hours_before = (appointment_time - cancellation_time).total_seconds() / 3600
          if hours_before >= policy_rules["free_cutoff_hours"]:
              return {"allowed": True, "penalty": 0.0, "reason": "within_free_window"}
          elif hours_before > 0:
              return {"allowed": True, "penalty": policy_rules["penalty_rate"], "reason": "late_cancellation"}
          else:
              return {"allowed": False, "penalty": policy_rules["no_show_rate"], "reason": "no_show"}
    dependencies: []
    verification_rule: "penalty_within_bounds"
    inputs: ["appointment_time:datetime", "cancellation_time:datetime", "policy_rules:Optional[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 5. INVENTORY (3 modules)
  # ===========================================================================
  - id: "stock_check"
    domain: "inventory"
    description: "Verifica el nivel de stock de un producto en uno o múltiples almacenes"
    code_block: |
      from typing import Dict, Any, List

      def stock_check(product_id: str, warehouse_inventory: Dict[str, Dict[str, Any]], warehouses: List[str] = None) -> Dict[str, Any]:
          """Check stock levels for a product across specified warehouses."""
          if warehouses is None:
              warehouses = list(warehouse_inventory.keys())
          total_quantity = 0
          details = {}
          for wh in warehouses:
              products = warehouse_inventory.get(wh, {})
              qty = products.get(product_id, {}).get("quantity", 0)
              details[wh] = qty
              total_quantity += qty
          return {"product_id": product_id, "total_quantity": total_quantity, "by_warehouse": details, "in_stock": total_quantity > 0}
    dependencies: []
    verification_rule: "quantity_non_negative"
    inputs: ["product_id:str", "warehouse_inventory:Dict[str,Dict[str,Any]]", "warehouses:List[str]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "reorder_alert"
    domain: "inventory"
    description: "Genera alertas de reorden cuando el stock cae por debajo del umbral mínimo definido"
    code_block: |
      from typing import Dict, Any, List

      def reorder_alert(inventory: Dict[str, Dict[str, Any]], default_threshold: int = 10) -> List[Dict[str, Any]]:
          """Generate reorder alerts for items below their minimum threshold."""
          alerts = []
          for product_id, data in inventory.items():
              quantity = data.get("quantity", 0)
              threshold = data.get("reorder_threshold", default_threshold)
              if quantity <= threshold:
                  alerts.append({"product_id": product_id, "current_quantity": quantity, "reorder_threshold": threshold, "deficit": threshold - quantity, "priority": "critical" if quantity == 0 else "warning"})
          return alerts
    dependencies: []
    verification_rule: "all_below_threshold_alerted"
    inputs: ["inventory:Dict[str,Dict[str,Any]]", "default_threshold:int"]
    outputs: ["alerts:List[Dict[str,Any]]"]

  - id: "warehouse_update"
    domain: "inventory"
    description: "Actualiza el inventario de un almacén aplicando transacciones de entrada y salida de stock"
    code_block: |
      from typing import Dict, Any, List
      from datetime import datetime, timezone

      def warehouse_update(warehouse_inventory: Dict[str, int], transactions: List[Dict[str, Any]]) -> Dict[str, Any]:
          """Apply stock transactions (in/out) to warehouse inventory and return updated state."""
          updated = warehouse_inventory.copy()
          applied = []
          for txn in transactions:
              product_id = txn["product_id"]
              delta = txn["quantity"]
              new_qty = updated.get(product_id, 0) + delta
              if new_qty < 0:
                  applied.append({"product_id": product_id, "status": "rejected", "reason": "insufficient_stock"})
                  continue
              updated[product_id] = new_qty
              applied.append({"product_id": product_id, "status": "applied", "new_quantity": new_qty})
          return {"updated_inventory": updated, "transactions_applied": applied, "timestamp": datetime.now(timezone.utc).isoformat()}
    dependencies: []
    verification_rule: "no_negative_stock"
    inputs: ["warehouse_inventory:Dict[str,int]", "transactions:List[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 6. ANALYTICS (3 modules)
  # ===========================================================================
  - id: "metrics_calculate"
    domain: "analytics"
    description: "Calcula métricas agregadas (suma, promedio, min, max, conteo) sobre una lista de valores numéricos"
    code_block: |
      from typing import List, Dict, Any, Optional
      import statistics

      def metrics_calculate(values: List[float], metric_names: Optional[List[str]] = None) -> Dict[str, Any]:
          """Calculate aggregate metrics over a list of numeric values."""
          if not values:
              return {"count": 0}
          all_metrics = {"count": len(values), "sum": sum(values), "mean": statistics.mean(values), "median": statistics.median(values), "min": min(values), "max": max(values), "stdev": statistics.stdev(values) if len(values) > 1 else 0.0}
          if metric_names:
              return {k: v for k, v in all_metrics.items() if k in metric_names}
          return all_metrics
    dependencies: []
    verification_rule: "mean_within_range"
    inputs: ["values:List[float]", "metric_names:Optional[List[str]]"]
    outputs: ["metrics:Dict[str,Any]"]

  - id: "report_generate"
    domain: "analytics"
    description: "Genera un informe estructurado con secciones, datos tabulares y resumen ejecutivo"
    code_block: |
      from datetime import datetime, timezone
      from typing import Dict, Any, List
      import uuid

      def report_generate(title: str, sections: List[Dict[str, Any]], summary: str, metadata: Optional[Dict[str, str]] = None) -> Dict[str, Any]:
          """Generate a structured report with sections and executive summary."""
          report = {"report_id": f"RPT-{uuid.uuid4().hex[:8].upper()}", "title": title, "generated_at": datetime.now(timezone.utc).isoformat(), "metadata": metadata or {}, "summary": summary, "sections": sections, "total_sections": len(sections)}
          return report
    dependencies: []
    verification_rule: "all_sections_present"
    inputs: ["title:str", "sections:List[Dict[str,Any]]", "summary:str", "metadata:Optional[Dict[str,str]]"]
    outputs: ["report:Dict[str,Any]"]

  - id: "dashboard_aggregate"
    domain: "analytics"
    description: "Agrega múltiples fuentes de datos en un formato unificado para visualización en dashboard"
    code_block: |
      from typing import Dict, Any, List

      def dashboard_aggregate(data_sources: Dict[str, List[Dict[str, Any]]], key_field: str, value_fields: List[str]) -> Dict[str, Any]:
          """Aggregate multiple data sources into a unified dashboard format keyed by key_field."""
          unified = {}
          for source_name, records in data_sources.items():
              for record in records:
                  key = record.get(key_field)
                  if key not in unified:
                      unified[key] = {key_field: key}
                  for vf in value_fields:
                      col_name = f"{source_name}_{vf}"
                      unified[key][col_name] = record.get(vf)
          return {"key_field": key_field, "records": list(unified.values()), "source_count": len(data_sources)}
    dependencies: []
    verification_rule: "all_sources_merged"
    inputs: ["data_sources:Dict[str,List[Dict[str,Any]]]", "key_field:str", "value_fields:List[str]"]
    outputs: ["dashboard:Dict[str,Any]"]

  # ===========================================================================
  # 7. CRM (3 modules)
  # ===========================================================================
  - id: "lead_score"
    domain: "crm"
    description: "Calcula la puntuación de un lead basándose en atributos demográficos y comportamentales"
    code_block: |
      from typing import Dict, Any

      def lead_score(lead: Dict[str, Any], scoring_model: Dict[str, float]) -> Dict[str, Any]:
          """Score a lead based on weighted demographic and behavioral attributes."""
          total_score = 0.0
          breakdown = {}
          for attribute, weight in scoring_model.items():
              value = lead.get(attribute, 0)
              if isinstance(value, bool):
                  value = 1.0 if value else 0.0
              score_contribution = float(value) * weight
              breakdown[attribute] = score_contribution
              total_score += score_contribution
          grade = "A" if total_score >= 80 else "B" if total_score >= 60 else "C" if total_score >= 40 else "D"
          return {"lead_id": lead.get("id"), "score": round(total_score, 2), "grade": grade, "breakdown": breakdown}
    dependencies: []
    verification_rule: "score_within_bounds"
    inputs: ["lead:Dict[str,Any]", "scoring_model:Dict[str,float]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "pipeline_advance"
    domain: "crm"
    description: "Avanza un deal en el pipeline de ventas validando criterios de transición entre etapas"
    code_block: |
      from typing import Dict, Any, List, Optional

      def pipeline_advance(deal: Dict[str, Any], target_stage: str, pipeline_stages: List[str], transition_rules: Optional[Dict[str, Dict[str, Any]]] = None) -> Dict[str, Any]:
          """Advance a deal to a target pipeline stage if transition rules are satisfied."""
          current_stage = deal.get("stage", pipeline_stages[0])
          if current_stage not in pipeline_stages or target_stage not in pipeline_stages:
              return {"success": False, "reason": "invalid_stage"}
          current_idx = pipeline_stages.index(current_stage)
          target_idx = pipeline_stages.index(target_stage)
          if target_idx <= current_idx:
              return {"success": False, "reason": "cannot_move_backward"}
          if transition_rules and target_stage in transition_rules:
              rules = transition_rules[target_stage]
              for field, required_value in rules.get("required_fields", {}).items():
                  if deal.get(field) != required_value:
                      return {"success": False, "reason": f"missing_requirement:{field}"}
          return {"success": True, "previous_stage": current_stage, "new_stage": target_stage, "stages_advanced": target_idx - current_idx}
    dependencies: []
    verification_rule: "backward_rejected"
    inputs: ["deal:Dict[str,Any]", "target_stage:str", "pipeline_stages:List[str]", "transition_rules:Optional[Dict[str,Dict[str,Any]]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "contact_merge"
    domain: "crm"
    description: "Fusiona dos contactos del CRM resolviendo conflictos con estrategia configurable (prefer_new, prefer_old, merge_all)"
    code_block: |
      from typing import Dict, Any

      def contact_merge(primary: Dict[str, Any], secondary: Dict[str, Any], strategy: str = "prefer_new") -> Dict[str, Any]:
          """Merge two CRM contacts using a conflict-resolution strategy."""
          merged = primary.copy()
          all_keys = set(list(primary.keys()) + list(secondary.keys()))
          for key in all_keys:
              p_val = primary.get(key)
              s_val = secondary.get(key)
              if p_val is None and s_val is not None:
                  merged[key] = s_val
              elif p_val is not None and s_val is not None and p_val != s_val:
                  if strategy == "prefer_new":
                      merged[key] = s_val
                  elif strategy == "prefer_old":
                      merged[key] = p_val
                  elif strategy == "merge_all":
                      merged[key] = [p_val, s_val] if not isinstance(p_val, list) else p_val + [s_val]
          merged["merged_from"] = secondary.get("id")
          merged["merge_strategy"] = strategy
          return merged
    dependencies: []
    verification_rule: "no_data_loss"
    inputs: ["primary:Dict[str,Any]", "secondary:Dict[str,Any]", "strategy:str"]
    outputs: ["merged:Dict[str,Any]"]

  # ===========================================================================
  # 8. AUTH (3 modules)
  # ===========================================================================
  - id: "oauth2_flow"
    domain: "auth"
    description: "Ejecuta el flujo OAuth2 Authorization Code intercambiando el código por tokens de acceso"
    code_block: |
      import httpx
      from typing import Dict, Any

      async def oauth2_flow(authorization_code: str, client_id: str, client_secret: str, redirect_uri: str, token_url: str) -> Dict[str, Any]:
          """Exchange an OAuth2 authorization code for access and refresh tokens."""
          payload = {"grant_type": "authorization_code", "code": authorization_code, "client_id": client_id, "client_secret": client_secret, "redirect_uri": redirect_uri}
          async with httpx.AsyncClient() as client:
              resp = await client.post(token_url, data=payload, timeout=30.0)
              resp.raise_for_status()
              token_data = resp.json()
              return {"access_token": token_data.get("access_token"), "refresh_token": token_data.get("refresh_token"), "token_type": token_data.get("token_type", "Bearer"), "expires_in": token_data.get("expires_in")}
    dependencies: ["httpx"]
    verification_rule: "access_token_returned"
    inputs: ["authorization_code:str", "client_id:str", "client_secret:str", "redirect_uri:str", "token_url:str"]
    outputs: ["tokens:Dict[str,Any]"]

  - id: "session_manage"
    domain: "auth"
    description: "Gestiona sesiones de usuario: crear, validar, refrescar y destruir con metadata configurable"
    code_block: |
      import uuid
      from datetime import datetime, timedelta, timezone
      from typing import Dict, Any, Optional

      def session_manage(action: str, session_store: Dict[str, Dict[str, Any]], user_id: str = "", session_id: str = "", ttl_minutes: int = 120, metadata: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
          """Create, validate, refresh, or destroy a user session."""
          if action == "create":
              sid = str(uuid.uuid4())
              session_store[sid] = {"user_id": user_id, "created_at": datetime.now(timezone.utc).isoformat(), "expires_at": (datetime.now(timezone.utc) + timedelta(minutes=ttl_minutes)).isoformat(), "metadata": metadata or {}}
              return {"action": "created", "session_id": sid}
          elif action == "validate":
              session = session_store.get(session_id)
              if not session:
                  return {"action": "validate", "valid": False, "reason": "not_found"}
              if datetime.now(timezone.utc) > datetime.fromisoformat(session["expires_at"]):
                  del session_store[session_id]
                  return {"action": "validate", "valid": False, "reason": "expired"}
              return {"action": "validate", "valid": True, "user_id": session["user_id"]}
          elif action == "destroy":
              session_store.pop(session_id, None)
              return {"action": "destroyed", "session_id": session_id}
          return {"action": action, "error": "unknown_action"}
    dependencies: []
    verification_rule: "expired_session_invalid"
    inputs: ["action:str", "session_store:Dict[str,Dict[str,Any]]", "user_id:str", "session_id:str", "ttl_minutes:int", "metadata:Optional[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "mfa_verify"
    domain: "auth"
    description: "Verifica un código TOTP de autenticación multi-factor usando el secreto compartido del usuario"
    code_block: |
      import pyotp
      from typing import Dict, Any

      def mfa_verify(secret: str, user_code: str, valid_window: int = 1) -> Dict[str, Any]:
          """Verify a TOTP-based MFA code against the user's shared secret."""
          totp = pyotp.TOTP(secret)
          verified = totp.verify(user_code, valid_window=valid_window)
          return {"verified": verified, "method": "totp"}
    dependencies: ["pyotp"]
    verification_rule: "invalid_code_rejected"
    inputs: ["secret:str", "user_code:str", "valid_window:int"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 9. DATA (3 modules)
  # ===========================================================================
  - id: "crud_create"
    domain: "data"
    description: "Crea un registro en un almacén de datos con validación de esquema, timestamps e ID generado"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional, List

      def crud_create(collection: Dict[str, Dict[str, Any]], data: Dict[str, Any], required_fields: Optional[List[str]] = None, schema: Optional[Dict[str, type]] = None) -> Dict[str, Any]:
          """Create a record in a collection with validation, timestamps, and generated ID."""
          if required_fields:
              missing = [f for f in required_fields if f not in data]
              if missing:
                  return {"success": False, "error": f"missing_fields:{missing}"}
          if schema:
              for field, expected_type in schema.items():
                  if field in data and not isinstance(data[field], expected_type):
                      return {"success": False, "error": f"type_mismatch:{field}"}
          record_id = str(uuid.uuid4())
          record = {"id": record_id, **data, "created_at": datetime.now(timezone.utc).isoformat(), "updated_at": datetime.now(timezone.utc).isoformat()}
          collection[record_id] = record
          return {"success": True, "record": record}
    dependencies: []
    verification_rule: "missing_fields_rejected"
    inputs: ["collection:Dict[str,Dict[str,Any]]", "data:Dict[str,Any]", "required_fields:Optional[List[str]]", "schema:Optional[Dict[str,type]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "migration_run"
    domain: "data"
    description: "Ejecuta una migración de datos aplicando transformaciones secuenciales con tracking de versión"
    code_block: |
      from typing import Dict, Any, List, Callable

      def migration_run(data: Dict[str, Any], migrations: List[Dict[str, Any]], current_version: int = 0) -> Dict[str, Any]:
          """Run sequential data migrations from current_version to latest."""
          result = data.copy()
          applied = []
          for migration in sorted(migrations, key=lambda m: m["version"]):
              if migration["version"] > current_version:
                  transform: Callable = migration["transform"]
                  result = transform(result)
                  applied.append({"version": migration["version"], "name": migration.get("name", "")})
          return {"data": result, "migrations_applied": applied, "new_version": max((m["version"] for m in applied), default=current_version)}
    dependencies: []
    verification_rule: "idempotent_re_run"
    inputs: ["data:Dict[str,Any]", "migrations:List[Dict[str,Any]]", "current_version:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "backup_execute"
    domain: "data"
    description: "Ejecuta un respaldo de datos serializándolos y almacenándolos con compresión y metadata de versión"
    code_block: |
      import json
      import gzip
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any

      def backup_execute(data: Dict[str, Any], backup_store: Dict[str, bytes], compress: bool = True) -> Dict[str, Any]:
          """Serialize, optionally compress, and store a data backup with metadata."""
          backup_id = f"bkp-{uuid.uuid4().hex[:8]}"
          serialized = json.dumps(data, default=str).encode("utf-8")
          if compress:
              stored = gzip.compress(serialized)
          else:
              stored = serialized
          backup_store[backup_id] = stored
          metadata = {"backup_id": backup_id, "created_at": datetime.now(timezone.utc).isoformat(), "original_size": len(serialized), "stored_size": len(stored), "compressed": compress, "record_count": len(data)}
          return {"success": True, "metadata": metadata}
    dependencies: []
    verification_rule: "restorable_from_backup"
    inputs: ["data:Dict[str,Any]", "backup_store:Dict[str,bytes]", "compress:bool"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 10. NOTIFICATIONS (3 modules)
  # ===========================================================================
  - id: "push_send"
    domain: "notifications"
    description: "Envía una notificación push a un dispositivo mediante Firebase Cloud Messaging"
    code_block: |
      import httpx
      from typing import Dict, Any, Optional

      async def push_send(device_token: str, title: str, body: str, server_key: str, data: Optional[Dict[str, str]] = None) -> Dict[str, Any]:
          """Send a push notification via Firebase Cloud Messaging."""
          url = "https://fcm.googleapis.com/fcm/send"
          headers = {"Authorization": f"key={server_key}", "Content-Type": "application/json"}
          payload = {"to": device_token, "notification": {"title": title, "body": body}}
          if data:
              payload["data"] = data
          async with httpx.AsyncClient() as client:
              resp = await client.post(url, json=payload, headers=headers, timeout=30.0)
              resp.raise_for_status()
              return resp.json()
    dependencies: ["httpx"]
    verification_rule: "success_count_positive"
    inputs: ["device_token:str", "title:str", "body:str", "server_key:str", "data:Optional[Dict[str,str]]"]
    outputs: ["response:Dict[str,Any]"]

  - id: "in_app_notify"
    domain: "notifications"
    description: "Crea una notificación in-app para un usuario con prioridad, categoría y estado de lectura"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def in_app_notify(user_id: str, notification_type: str, message: str, priority: str = "normal", category: str = "general", action_url: Optional[str] = None) -> Dict[str, Any]:
          """Create an in-app notification for a user with priority and read status."""
          notification = {"id": str(uuid.uuid4()), "user_id": user_id, "type": notification_type, "message": message, "priority": priority, "category": category, "action_url": action_url, "read": False, "created_at": datetime.now(timezone.utc).isoformat()}
          return notification
    dependencies: []
    verification_rule: "unread_by_default"
    inputs: ["user_id:str", "notification_type:str", "message:str", "priority:str", "category:str", "action_url:Optional[str]"]
    outputs: ["notification:Dict[str,Any]"]

  - id: "digest_compile"
    domain: "notifications"
    description: "Compila un resumen periódico de notificaciones agrupándolas por categoría y prioridad"
    code_block: |
      from typing import List, Dict, Any
      from collections import defaultdict

      def digest_compile(notifications: List[Dict[str, Any]], group_by: str = "category") -> Dict[str, Any]:
          """Compile a digest of notifications grouped by a specified field."""
          groups = defaultdict(list)
          unread_count = 0
          for notif in notifications:
              key = notif.get(group_by, "ungrouped")
              groups[key].append(notif)
              if not notif.get("read", False):
                  unread_count += 1
          digest = {}
          for group_key, items in groups.items():
              digest[group_key] = {"count": len(items), "unread": sum(1 for i in items if not i.get("read", False)), "items": items}
          return {"total": len(notifications), "unread_total": unread_count, "groups": dict(digest)}
    dependencies: []
    verification_rule: "counts_consistent"
    inputs: ["notifications:List[Dict[str,Any]]", "group_by:str"]
    outputs: ["digest:Dict[str,Any]"]

  # ===========================================================================
  # 11. ECOMMERCE (4 modules)
  # ===========================================================================
  - id: "cart_calculate"
    domain: "ecommerce"
    description: "Calcula los totales del carrito incluyendo subtotal, impuestos, descuentos y total final"
    code_block: |
      from typing import List, Dict, Any, Optional

      def cart_calculate(items: List[Dict[str, Any]], tax_rate: float = 0.0, discount_code: Optional[Dict[str, Any]] = None, shipping_cost: float = 0.0) -> Dict[str, Any]:
          """Calculate cart totals including subtotal, tax, discounts, and final total."""
          subtotal = sum(item["price"] * item["quantity"] for item in items)
          discount_amount = 0.0
          if discount_code:
              if discount_code.get("type") == "percentage":
                  discount_amount = subtotal * (discount_code["value"] / 100)
              elif discount_code.get("type") == "fixed":
                  discount_amount = min(discount_code["value"], subtotal)
          taxable = subtotal - discount_amount
          tax_amount = round(taxable * tax_rate, 2)
          total = round(taxable + tax_amount + shipping_cost, 2)
          return {"subtotal": round(subtotal, 2), "discount": round(discount_amount, 2), "tax": tax_amount, "shipping": shipping_cost, "total": total, "item_count": sum(i["quantity"] for i in items)}
    dependencies: []
    verification_rule: "total_equals_components"
    inputs: ["items:List[Dict[str,Any]]", "tax_rate:float", "discount_code:Optional[Dict[str,Any]]", "shipping_cost:float"]
    outputs: ["totals:Dict[str,Any]"]

  - id: "checkout_process"
    domain: "ecommerce"
    description: "Procesa el checkout validando inventario, aplicando pagos y creando la orden"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, List, Optional

      def checkout_process(cart: Dict[str, Any], payment_method: Dict[str, Any], shipping_address: Dict[str, str], inventory_check: Optional[Dict[str, int]] = None) -> Dict[str, Any]:
          """Process checkout: validate inventory, create order, and confirm payment."""
          if inventory_check:
              for item in cart.get("items", []):
                  available = inventory_check.get(item["product_id"], 0)
                  if available < item["quantity"]:
                      return {"success": False, "reason": "insufficient_inventory", "product_id": item["product_id"]}
          order_id = f"ORD-{uuid.uuid4().hex[:10].upper()}"
          order = {"order_id": order_id, "items": cart["items"], "totals": cart.get("totals", {}), "payment": {"method": payment_method["type"], "last4": payment_method.get("last4", "")}, "shipping_address": shipping_address, "status": "confirmed", "created_at": datetime.now(timezone.utc).isoformat()}
          return {"success": True, "order": order}
    dependencies: []
    verification_rule: "order_id_generated"
    inputs: ["cart:Dict[str,Any]", "payment_method:Dict[str,Any]", "shipping_address:Dict[str,str]", "inventory_check:Optional[Dict[str,int]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "shipping_estimate"
    domain: "ecommerce"
    description: "Estima costos y tiempos de envío basándose en peso, dimensiones, origen y destino"
    code_block: |
      from typing import Dict, Any, List

      def shipping_estimate(weight_kg: float, dimensions_cm: Dict[str, float], origin: str, destination: str, carriers: List[Dict[str, Any]] = None) -> List[Dict[str, Any]]:
          """Estimate shipping costs and delivery times from available carriers."""
          if carriers is None:
              carriers = [{"name": "standard", "rate_per_kg": 5.0, "base_days": 7}, {"name": "express", "rate_per_kg": 12.0, "base_days": 3}]
          volume = dimensions_cm.get("length", 1) * dimensions_cm.get("width", 1) * dimensions_cm.get("height", 1)
          volumetric_weight = volume / 5000
          billable_weight = max(weight_kg, volumetric_weight)
          estimates = []
          for carrier in carriers:
              cost = round(billable_weight * carrier["rate_per_kg"], 2)
              estimates.append({"carrier": carrier["name"], "estimated_cost": cost, "estimated_days": carrier["base_days"], "billable_weight_kg": round(billable_weight, 2)})
          return estimates
    dependencies: []
    verification_rule: "volumetric_weight_applied"
    inputs: ["weight_kg:float", "dimensions_cm:Dict[str,float]", "origin:str", "destination:str", "carriers:List[Dict[str,Any]]"]
    outputs: ["estimates:List[Dict[str,Any]]"]

  - id: "tax_calculate"
    domain: "ecommerce"
    description: "Calcula impuestos aplicables basándose en jurisdicción, categoría de producto y reglas fiscales"
    code_block: |
      from typing import Dict, Any, List

      def tax_calculate(amount: float, jurisdiction: str, product_category: str, tax_rules: Dict[str, Dict[str, Any]], exemptions: List[str] = None) -> Dict[str, Any]:
          """Calculate applicable taxes based on jurisdiction, product category, and tax rules."""
          if exemptions and product_category in exemptions:
              return {"total_tax": 0.0, "effective_rate": 0.0, "breakdown": [], "exempt": True}
          rules = tax_rules.get(jurisdiction, {})
          rate = rules.get(product_category, rules.get("default", 0.0))
          tax_amount = round(amount * rate, 2)
          breakdown = [{"jurisdiction": jurisdiction, "category": product_category, "rate": rate, "amount": tax_amount}]
          return {"total_tax": tax_amount, "effective_rate": rate, "breakdown": breakdown, "exempt": False}
    dependencies: []
    verification_rule: "exempt_category_zero_tax"
    inputs: ["amount:float", "jurisdiction:str", "product_category:str", "tax_rules:Dict[str,Dict[str,Any]]", "exemptions:List[str]"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 12. HEALTHCARE (4 modules)
  # ===========================================================================
  - id: "patient_register"
    domain: "healthcare"
    description: "Registra un nuevo paciente con validación de datos demográficos y asignación de ID médico"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, List, Optional

      def patient_register(patient_data: Dict[str, Any], required_fields: Optional[List[str]] = None) -> Dict[str, Any]:
          """Register a new patient with validation and medical ID assignment."""
          if required_fields is None:
              required_fields = ["first_name", "last_name", "date_of_birth"]
          missing = [f for f in required_fields if f not in patient_data or not patient_data[f]]
          if missing:
              return {"success": False, "error": f"missing_required:{missing}"}
          patient_id = f"PAT-{uuid.uuid4().hex[:8].upper()}"
          record = {"patient_id": patient_id, **patient_data, "registered_at": datetime.now(timezone.utc).isoformat(), "status": "active", "allergies": patient_data.get("allergies", []), "emergency_contact": patient_data.get("emergency_contact", {})}
          return {"success": True, "patient": record}
    dependencies: []
    verification_rule: "missing_fields_rejected"
    inputs: ["patient_data:Dict[str,Any]", "required_fields:Optional[List[str]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "prescription_validate"
    domain: "healthcare"
    description: "Valida una prescripción verificando dosis, interacciones medicamentosas y restricciones del paciente"
    code_block: |
      from typing import Dict, Any, List

      def prescription_validate(prescription: Dict[str, Any], patient: Dict[str, Any], drug_database: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
          """Validate a prescription checking dosage, interactions, and patient restrictions."""
          warnings = []
          errors = []
          for med in prescription.get("medications", []):
              drug_info = drug_database.get(med["drug_name"])
              if not drug_info:
                  errors.append(f"unknown_drug:{med['drug_name']}")
                  continue
              if med["dosage"] > drug_info.get("max_dosage", float("inf")):
                  errors.append(f"overdose:{med['drug_name']}")
              for allergy in patient.get("allergies", []):
                  if allergy.lower() in drug_info.get("allergen_tags", []):
                      errors.append(f"allergy_conflict:{med['drug_name']}")
              for existing_med in patient.get("current_medications", []):
                  if existing_med in drug_info.get("interactions", []):
                      warnings.append(f"interaction:{med['drug_name']}+{existing_med}")
          return {"valid": len(errors) == 0, "errors": errors, "warnings": warnings}
    dependencies: []
    verification_rule: "allergy_conflict_detected"
    inputs: ["prescription:Dict[str,Any]", "patient:Dict[str,Any]", "drug_database:Dict[str,Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "appointment_triage"
    domain: "healthcare"
    description: "Realiza triaje de citas médicas clasificando urgencia según síntomas y signos vitales"
    code_block: |
      from typing import Dict, Any, List

      def appointment_triage(symptoms: List[str], vitals: Dict[str, float], triage_rules: Dict[str, Any] = None) -> Dict[str, Any]:
          """Triage a medical appointment based on symptoms and vitals to determine urgency."""
          if triage_rules is None:
              triage_rules = {"critical_symptoms": ["chest_pain", "difficulty_breathing", "unconscious"], "critical_vitals": {"heart_rate_max": 130, "heart_rate_min": 40, "systolic_max": 180, "temperature_max": 39.5}}
          urgency = "routine"
          reasons = []
          for symptom in symptoms:
              if symptom in triage_rules.get("critical_symptoms", []):
                  urgency = "emergency"
                  reasons.append(f"critical_symptom:{symptom}")
          hr = vitals.get("heart_rate", 70)
          if hr > triage_rules["critical_vitals"]["heart_rate_max"] or hr < triage_rules["critical_vitals"]["heart_rate_min"]:
              urgency = "urgent" if urgency != "emergency" else urgency
              reasons.append(f"abnormal_heart_rate:{hr}")
          if vitals.get("temperature", 37.0) > triage_rules["critical_vitals"]["temperature_max"]:
              urgency = "urgent" if urgency != "emergency" else urgency
              reasons.append(f"high_fever:{vitals['temperature']}")
          return {"urgency": urgency, "reasons": reasons, "symptoms": symptoms, "vitals": vitals}
    dependencies: []
    verification_rule: "critical_symptom_emergency"
    inputs: ["symptoms:List[str]", "vitals:Dict[str,float]", "triage_rules:Dict[str,Any]"]
    outputs: ["triage:Dict[str,Any]"]

  - id: "vital_alert"
    domain: "healthcare"
    description: "Genera alertas cuando los signos vitales de un paciente exceden los umbrales críticos definidos"
    code_block: |
      from typing import Dict, Any, List

      def vital_alert(patient_id: str, vitals: Dict[str, float], thresholds: Dict[str, Dict[str, float]]) -> List[Dict[str, Any]]:
          """Generate alerts when patient vitals exceed defined critical thresholds."""
          alerts = []
          for vital_name, value in vitals.items():
              rules = thresholds.get(vital_name)
              if not rules:
                  continue
              if "min" in rules and value < rules["min"]:
                  alerts.append({"patient_id": patient_id, "vital": vital_name, "value": value, "direction": "low", "threshold": rules["min"], "severity": "critical" if value < rules.get("critical_min", rules["min"]) else "warning"})
              if "max" in rules and value > rules["max"]:
                  alerts.append({"patient_id": patient_id, "vital": vital_name, "value": value, "direction": "high", "threshold": rules["max"], "severity": "critical" if value > rules.get("critical_max", rules["max"]) else "warning"})
          return alerts
    dependencies: []
    verification_rule: "threshold_breach_alerted"
    inputs: ["patient_id:str", "vitals:Dict[str,float]", "thresholds:Dict[str,Dict[str,float]]"]
    outputs: ["alerts:List[Dict[str,Any]]"]

  # ===========================================================================
  # 13. EDUCATION (3 modules)
  # ===========================================================================
  - id: "enrollment_process"
    domain: "education"
    description: "Procesa la matrícula de un estudiante verificando prerequisitos, cupo y generando el registro"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, List

      def enrollment_process(student: Dict[str, Any], course: Dict[str, Any], completed_courses: List[str], enrollment_capacity: Dict[str, int]) -> Dict[str, Any]:
          """Process a student enrollment checking prerequisites and capacity."""
          prerequisites = course.get("prerequisites", [])
          unmet = [p for p in prerequisites if p not in completed_courses]
          if unmet:
              return {"success": False, "reason": "prerequisites_unmet", "missing": unmet}
          course_id = course["id"]
          current_enrolled = enrollment_capacity.get(f"{course_id}_enrolled", 0)
          max_capacity = enrollment_capacity.get(f"{course_id}_max", 0)
          if current_enrolled >= max_capacity:
              return {"success": False, "reason": "course_full"}
          enrollment_id = f"ENR-{uuid.uuid4().hex[:8].upper()}"
          return {"success": True, "enrollment_id": enrollment_id, "student_id": student["id"], "course_id": course_id, "enrolled_at": datetime.now(timezone.utc).isoformat(), "status": "active"}
    dependencies: []
    verification_rule: "prerequisite_unmet_rejected"
    inputs: ["student:Dict[str,Any]", "course:Dict[str,Any]", "completed_courses:List[str]", "enrollment_capacity:Dict[str,int]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "grade_calculate"
    domain: "education"
    description: "Calcula la calificación final de un estudiante ponderando componentes evaluativos con redondeo configurable"
    code_block: |
      from typing import Dict, Any, List

      def grade_calculate(grades: Dict[str, float], weight_scheme: Dict[str, float], scale: str = "100", round_to: int = 1) -> Dict[str, Any]:
          """Calculate a weighted final grade from component grades and a weight scheme."""
          total_weight = sum(weight_scheme.values())
          if abs(total_weight - 1.0) > 0.01:
              return {"success": False, "error": "weights_do_not_sum_to_1", "total_weight": total_weight}
          weighted_sum = 0.0
          breakdown = []
          for component, grade in grades.items():
              weight = weight_scheme.get(component, 0.0)
              contribution = grade * weight
              weighted_sum += contribution
              breakdown.append({"component": component, "grade": grade, "weight": weight, "contribution": round(contribution, round_to)})
          final_grade = round(weighted_sum, round_to)
          letter = "A" if final_grade >= 90 else "B" if final_grade >= 80 else "C" if final_grade >= 70 else "D" if final_grade >= 60 else "F"
          return {"success": True, "final_grade": final_grade, "letter_grade": letter, "breakdown": breakdown}
    dependencies: []
    verification_rule: "weights_sum_to_one"
    inputs: ["grades:Dict[str,float]", "weight_scheme:Dict[str,float]", "scale:str", "round_to:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "certificate_generate"
    domain: "education"
    description: "Genera un certificado de finalización verificable con código único y datos del curso"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any

      def certificate_generate(student_name: str, course_name: str, completion_date: str, issuer: str, grade: str = "") -> Dict[str, Any]:
          """Generate a completion certificate with a unique verification code."""
          cert_id = f"CERT-{uuid.uuid4().hex[:12].upper()}"
          verification_code = uuid.uuid4().hex
          certificate = {"certificate_id": cert_id, "verification_code": verification_code, "student_name": student_name, "course_name": course_name, "completion_date": completion_date, "grade": grade, "issuer": issuer, "issued_at": datetime.now(timezone.utc).isoformat(), "status": "valid"}
          return certificate
    dependencies: []
    verification_rule: "verification_code_unique"
    inputs: ["student_name:str", "course_name:str", "completion_date:str", "issuer:str", "grade:str"]
    outputs: ["certificate:Dict[str,Any]"]

  # ===========================================================================
  # 14. FINANCE (4 modules)
  # ===========================================================================
  - id: "interest_calculate"
    domain: "finance"
    description: "Calcula interés simple o compuesto con frecuencia de capitalización configurable"
    code_block: |
      from typing import Dict, Any

      def interest_calculate(principal: float, annual_rate: float, periods: int, compounding: str = "monthly") -> Dict[str, Any]:
          """Calculate simple or compound interest over a number of periods."""
          freq_map = {"annually": 1, "semi_annually": 2, "quarterly": 4, "monthly": 12, "daily": 365}
          n = freq_map.get(compounding, 12)
          rate_per_period = annual_rate / n
          total_periods = periods * n
          compound_amount = principal * (1 + rate_per_period) ** total_periods
          compound_interest = compound_amount - principal
          simple_interest = principal * annual_rate * periods
          return {"principal": principal, "annual_rate": annual_rate, "periods": periods, "compounding": compounding, "compound_interest": round(compound_interest, 2), "compound_amount": round(compound_amount, 2), "simple_interest": round(simple_interest, 2)}
    dependencies: []
    verification_rule: "compound_gt_simple"
    inputs: ["principal:float", "annual_rate:float", "periods:int", "compounding:str"]
    outputs: ["result:Dict[str,Any]"]

  - id: "credit_score"
    domain: "finance"
    description: "Calcula un score crediticio basado en factores de pago, utilización, antigüedad y consultas"
    code_block: |
      from typing import Dict, Any

      def credit_score(factors: Dict[str, Any]) -> Dict[str, Any]:
          """Calculate a credit score from payment history, utilization, age, and inquiries."""
          payment_weight = 0.35
          utilization_weight = 0.30
          age_weight = 0.15
          inquiries_weight = 0.10
          mix_weight = 0.10
          payment_score = factors.get("on_time_payment_rate", 1.0) * 850
          utilization = factors.get("credit_utilization", 0.0)
          utilization_score = max(0, (1 - utilization) * 850)
          age_score = min(factors.get("credit_age_months", 0) / 240, 1.0) * 850
          inquiries = factors.get("recent_inquiries", 0)
          inquiries_score = max(0, 850 - inquiries * 30)
          mix_score = min(factors.get("credit_mix_types", 0) / 4, 1.0) * 850
          raw = (payment_score * payment_weight + utilization_score * utilization_weight + age_score * age_weight + inquiries_score * inquiries_weight + mix_score * mix_weight)
          score = max(300, min(850, int(raw)))
          rating = "excellent" if score >= 750 else "good" if score >= 700 else "fair" if score >= 650 else "poor"
          return {"score": score, "rating": rating, "factors": factors}
    dependencies: []
    verification_rule: "score_between_300_850"
    inputs: ["factors:Dict[str,Any]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "amortization_schedule"
    domain: "finance"
    description: "Genera una tabla de amortización para un préstamo con pagos mensuales fijos"
    code_block: |
      from typing import List, Dict, Any

      def amortization_schedule(principal: float, annual_rate: float, term_months: int) -> Dict[str, Any]:
          """Generate an amortization schedule for a fixed-rate loan."""
          monthly_rate = annual_rate / 12
          if monthly_rate == 0:
              monthly_payment = principal / term_months
          else:
              monthly_payment = principal * (monthly_rate * (1 + monthly_rate) ** term_months) / ((1 + monthly_rate) ** term_months - 1)
          schedule = []
          balance = principal
          for month in range(1, term_months + 1):
              interest = round(balance * monthly_rate, 2)
              principal_paid = round(monthly_payment - interest, 2)
              balance = round(balance - principal_paid, 2)
              if balance < 0:
                  balance = 0
              schedule.append({"month": month, "payment": round(monthly_payment, 2), "principal": principal_paid, "interest": interest, "balance": balance})
          return {"monthly_payment": round(monthly_payment, 2), "total_paid": round(monthly_payment * term_months, 2), "total_interest": round(monthly_payment * term_months - principal, 2), "schedule": schedule}
    dependencies: []
    verification_rule: "final_balance_zero"
    inputs: ["principal:float", "annual_rate:float", "term_months:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "transaction_reconcile"
    domain: "finance"
    description: "Concilia transacciones internas con extractos bancarios identificando discrepancias"
    code_block: |
      from typing import List, Dict, Any
      from decimal import Decimal

      def transaction_reconcile(internal_transactions: List[Dict[str, Any]], bank_statements: List[Dict[str, Any]], tolerance: float = 0.01) -> Dict[str, Any]:
          """Reconcile internal transactions against bank statements within a tolerance."""
          matched = []
          unmatched_internal = []
          unmatched_bank = list(bank_statements)
          for txn in internal_transactions:
              found = False
              for i, bank_txn in enumerate(unmatched_bank):
                  if txn.get("reference") == bank_txn.get("reference") and abs(Decimal(str(txn["amount"])) - Decimal(str(bank_txn["amount"]))) <= Decimal(str(tolerance)):
                      matched.append({"internal": txn, "bank": bank_txn})
                      unmatched_bank.pop(i)
                      found = True
                      break
              if not found:
                  unmatched_internal.append(txn)
          return {"matched_count": len(matched), "unmatched_internal_count": len(unmatched_internal), "unmatched_bank_count": len(unmatched_bank), "unmatched_internal": unmatched_internal, "unmatched_bank": unmatched_bank}
    dependencies: []
    verification_rule: "all_matched_when_consistent"
    inputs: ["internal_transactions:List[Dict[str,Any]]", "bank_statements:List[Dict[str,Any]]", "tolerance:float"]
    outputs: ["reconciliation:Dict[str,Any]"]

  # ===========================================================================
  # 15. LEGAL (3 modules)
  # ===========================================================================
  - id: "contract_validate"
    domain: "legal"
    description: "Valida un contrato verificando cláusulas obligatorias, firmas y fechas de vigencia"
    code_block: |
      from datetime import datetime
      from typing import Dict, Any, List

      def contract_validate(contract: Dict[str, Any], required_clauses: List[str] = None, required_signatures: List[str] = None) -> Dict[str, Any]:
          """Validate a contract checking mandatory clauses, signatures, and effective dates."""
          if required_clauses is None:
              required_clauses = ["parties", "consideration", "term", "governing_law"]
          if required_signatures is None:
              required_signatures = ["party_a", "party_b"]
          issues = []
          present_clauses = contract.get("clauses", {})
          missing_clauses = [c for c in required_clauses if c not in present_clauses]
          if missing_clauses:
              issues.append({"type": "missing_clause", "details": missing_clauses})
          signatures = contract.get("signatures", {})
          missing_sigs = [s for s in required_signatures if s not in signatures or not signatures[s]]
          if missing_sigs:
              issues.append({"type": "missing_signature", "details": missing_sigs})
          effective = contract.get("effective_date")
          if effective:
              eff_date = effective if isinstance(effective, datetime) else datetime.fromisoformat(effective)
              if eff_date > datetime.now(eff_date.tzinfo):
                  issues.append({"type": "not_yet_effective", "date": str(effective)})
          return {"valid": len(issues) == 0, "issues": issues}
    dependencies: []
    verification_rule: "missing_clause_flagged"
    inputs: ["contract:Dict[str,Any]", "required_clauses:List[str]", "required_signatures:List[str]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "deadline_track"
    domain: "legal"
    description: "Rastrea plazos legales calculando fechas límite y alertando sobre vencimientos próximos"
    code_block: |
      from datetime import datetime, timedelta, timezone
      from typing import Dict, Any, List

      def deadline_track(deadlines: List[Dict[str, Any]], warning_days: int = 7, reference_date: datetime = None) -> Dict[str, Any]:
          """Track legal deadlines, compute remaining days, and flag upcoming ones."""
          if reference_date is None:
              reference_date = datetime.now(timezone.utc)
          tracked = []
          upcoming = []
          overdue = []
          for dl in deadlines:
              due = dl["due_date"] if isinstance(dl["due_date"], datetime) else datetime.fromisoformat(dl["due_date"])
              remaining = (due - reference_date).days
              entry = {**dl, "remaining_days": remaining, "status": "overdue" if remaining < 0 else "upcoming" if remaining <= warning_days else "on_track"}
              tracked.append(entry)
              if remaining < 0:
                  overdue.append(entry)
              elif remaining <= warning_days:
                  upcoming.append(entry)
          return {"total": len(tracked), "overdue_count": len(overdue), "upcoming_count": len(upcoming), "tracked": tracked}
    dependencies: []
    verification_rule: "overdue_detected"
    inputs: ["deadlines:List[Dict[str,Any]]", "warning_days:int", "reference_date:datetime"]
    outputs: ["result:Dict[str,Any]"]

  - id: "compliance_check"
    domain: "legal"
    description: "Verifica el cumplimiento de regulaciones específicas contra un conjunto de datos de la organización"
    code_block: |
      from typing import Dict, Any, List

      def compliance_check(organization_data: Dict[str, Any], regulations: List[Dict[str, Any]]) -> Dict[str, Any]:
          """Check organizational data against regulatory compliance requirements."""
          violations = []
          compliant_items = []
          for reg in regulations:
              reg_id = reg["id"]
              checks = reg.get("checks", [])
              reg_violations = []
              for check in checks:
                  field = check["field"]
                  condition = check["condition"]
                  value = organization_data.get(field)
                  if condition == "required" and not value:
                      reg_violations.append({"field": field, "issue": "missing_required"})
                  elif condition == "min_length" and value and len(str(value)) < check["value"]:
                      reg_violations.append({"field": field, "issue": f"below_min_length:{check['value']}"})
                  elif condition == "one_of" and value not in check["values"]:
                      reg_violations.append({"field": field, "issue": f"invalid_value"})
              if reg_violations:
                  violations.append({"regulation_id": reg_id, "issues": reg_violations})
              else:
                  compliant_items.append(reg_id)
          return {"compliant": len(violations) == 0, "violations": violations, "compliant_regulations": compliant_items}
    dependencies: []
    verification_rule: "missing_required_flagged"
    inputs: ["organization_data:Dict[str,Any]", "regulations:List[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 16. LOGISTICS (3 modules)
  # ===========================================================================
  - id: "route_optimize"
    domain: "logistics"
    description: "Optimiza la ruta de entrega usando el algoritmo del vecino más cercano para múltiples paradas"
    code_block: |
      import math
      from typing import List, Dict, Any

      def route_optimize(locations: List[Dict[str, float]], start_index: int = 0) -> Dict[str, Any]:
          """Optimize delivery route using nearest-neighbor heuristic for multiple stops."""
          if len(locations) <= 1:
              return {"route": locations, "total_distance": 0.0, "stops": len(locations)}
          unvisited = set(range(len(locations)))
          route_indices = [start_index]
          unvisited.discard(start_index)
          total_distance = 0.0
          current = start_index
          while unvisited:
              nearest = min(unvisited, key=lambda i: math.hypot(locations[i]["lat"] - locations[current]["lat"], locations[i]["lng"] - locations[current]["lng"]))
              dist = math.hypot(locations[nearest]["lat"] - locations[current]["lat"], locations[nearest]["lng"] - locations[current]["lng"])
              total_distance += dist
              route_indices.append(nearest)
              unvisited.discard(nearest)
              current = nearest
          return {"route_indices": route_indices, "route": [locations[i] for i in route_indices], "total_distance": round(total_distance, 2), "stops": len(route_indices)}
    dependencies: []
    verification_rule: "all_locations_visited"
    inputs: ["locations:List[Dict[str,float]]", "start_index:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "shipment_track"
    domain: "logistics"
    description: "Rastrea un envío devolviendo su estado actual, ubicación e historial de eventos"
    code_block: |
      from typing import Dict, Any, List, Optional

      def shipment_track(tracking_id: str, shipment_database: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
          """Track a shipment returning current status, location, and event history."""
          shipment = shipment_database.get(tracking_id)
          if not shipment:
              return {"found": False, "tracking_id": tracking_id}
          events = shipment.get("events", [])
          latest_event = events[-1] if events else None
          return {"found": True, "tracking_id": tracking_id, "status": shipment.get("status", "unknown"), "carrier": shipment.get("carrier", ""), "origin": shipment.get("origin"), "destination": shipment.get("destination"), "latest_event": latest_event, "event_count": len(events), "estimated_delivery": shipment.get("estimated_delivery")}
    dependencies: []
    verification_rule: "unknown_tracking_not_found"
    inputs: ["tracking_id:str", "shipment_database:Dict[str,Dict[str,Any]]"]
    outputs: ["tracking:Dict[str,Any]"]

  - id: "delivery_confirm"
    domain: "logistics"
    description: "Confirma la entrega de un envío registrando fecha, ubicación y firma o evidencia"
    code_block: |
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def delivery_confirm(tracking_id: str, delivered_by: str, delivery_location: Dict[str, float], signature: Optional[str] = None, photo_evidence: Optional[str] = None, notes: str = "") -> Dict[str, Any]:
          """Confirm delivery of a shipment with timestamp, location, and evidence."""
          confirmation = {"tracking_id": tracking_id, "status": "delivered", "delivered_at": datetime.now(timezone.utc).isoformat(), "delivered_by": delivered_by, "delivery_location": delivery_location, "signature": signature, "photo_evidence": photo_evidence, "notes": notes, "confirmed": True}
          return confirmation
    dependencies: []
    verification_rule: "status_is_delivered"
    inputs: ["tracking_id:str", "delivered_by:str", "delivery_location:Dict[str,float]", "signature:Optional[str]", "photo_evidence:Optional[str]", "notes:str"]
    outputs: ["confirmation:Dict[str,Any]"]

  # ===========================================================================
  # 17. HOSPITALITY (3 modules)
  # ===========================================================================
  - id: "reservation_manage"
    domain: "hospitality"
    description: "Gestiona reservas hoteleras: crear, modificar y cancelar con validación de disponibilidad"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def reservation_manage(action: str, guest_id: str, room_type: str, check_in: str, check_out: str, existing_reservations: Dict[str, Dict[str, Any]], room_inventory: Dict[str, int], modification: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
          """Create, modify, or cancel a hotel reservation with availability validation."""
          if action == "create":
              available = room_inventory.get(room_type, 0)
              occupied = sum(1 for r in existing_reservations.values() if r["room_type"] == room_type and r["status"] == "confirmed" and not (check_out <= r["check_in"] or check_in >= r["check_out"]))
              if occupied >= available:
                  return {"success": False, "reason": "no_availability"}
              res_id = f"RES-{uuid.uuid4().hex[:8].upper()}"
              return {"success": True, "reservation_id": res_id, "guest_id": guest_id, "room_type": room_type, "check_in": check_in, "check_out": check_out, "status": "confirmed"}
          elif action == "cancel":
              res_id = modification.get("reservation_id", "") if modification else ""
              if res_id in existing_reservations:
                  return {"success": True, "reservation_id": res_id, "status": "cancelled"}
              return {"success": False, "reason": "not_found"}
          return {"success": False, "reason": "unknown_action"}
    dependencies: []
    verification_rule: "no_double_booking"
    inputs: ["action:str", "guest_id:str", "room_type:str", "check_in:str", "check_out:str", "existing_reservations:Dict[str,Dict[str,Any]]", "room_inventory:Dict[str,int]", "modification:Optional[Dict[str,Any]]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "room_availability"
    domain: "hospitality"
    description: "Consulta la disponibilidad de habitaciones por tipo y rango de fechas"
    code_block: |
      from typing import Dict, Any, List

      def room_availability(room_inventory: Dict[str, int], reservations: List[Dict[str, Any]], check_in: str, check_out: str) -> Dict[str, Any]:
          """Check room availability by type for a given date range."""
          availability = {}
          for room_type, total in room_inventory.items():
              occupied = sum(1 for r in reservations if r.get("room_type") == room_type and r.get("status") == "confirmed" and not (check_out <= r["check_in"] or check_in >= r["check_out"]))
              available = total - occupied
              availability[room_type] = {"total": total, "occupied": occupied, "available": available, "can_book": available > 0}
          return {"check_in": check_in, "check_out": check_out, "availability": availability}
    dependencies: []
    verification_rule: "availability_non_negative"
    inputs: ["room_inventory:Dict[str,int]", "reservations:List[Dict[str,Any]]", "check_in:str", "check_out:str"]
    outputs: ["result:Dict[str,Any]"]

  - id: "guest_checkin"
    domain: "hospitality"
    description: "Procesa el check-in de un huésped asignando habitación, verificando reserva y generando llave"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def guest_checkin(reservation_id: str, guest_data: Dict[str, Any], reservation_db: Dict[str, Dict[str, Any]], assigned_room: Optional[str] = None) -> Dict[str, Any]:
          """Process guest check-in: verify reservation, assign room, and generate key code."""
          reservation = reservation_db.get(reservation_id)
          if not reservation:
              return {"success": False, "reason": "reservation_not_found"}
          if reservation.get("status") != "confirmed":
              return {"success": False, "reason": f"invalid_status:{reservation.get('status')}"}
          room = assigned_room or reservation.get("room_number", f"RM-{uuid.uuid4().hex[:4]}")
          key_code = uuid.uuid4().hex[:8].upper()
          checkin = {"success": True, "reservation_id": reservation_id, "guest_name": guest_data.get("name", ""), "room": room, "key_code": key_code, "checked_in_at": datetime.now(timezone.utc).isoformat(), "status": "checked_in"}
          return checkin
    dependencies: []
    verification_rule: "invalid_reservation_rejected"
    inputs: ["reservation_id:str", "guest_data:Dict[str,Any]", "reservation_db:Dict[str,Dict[str,Any]]", "assigned_room:Optional[str]"]
    outputs: ["checkin:Dict[str,Any]"]

  # ===========================================================================
  # 18. MEDIA (3 modules)
  # ===========================================================================
  - id: "content_publish"
    domain: "media"
    description: "Publica contenido verificando estado de revisión, programación y metadatos requeridos"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def content_publish(content: Dict[str, Any], review_status: str = "approved", scheduled_time: Optional[str] = None, required_metadata: Optional[list] = None) -> Dict[str, Any]:
          """Publish content after verifying review status, scheduling, and required metadata."""
          if review_status != "approved":
              return {"success": False, "reason": f"not_approved:{review_status}"}
          if required_metadata:
              missing = [m for m in required_metadata if m not in content.get("metadata", {})]
              if missing:
                  return {"success": False, "reason": f"missing_metadata:{missing}"}
          publish_time = scheduled_time or datetime.now(timezone.utc).isoformat()
          content_id = content.get("id", f"PUB-{uuid.uuid4().hex[:8].upper()}")
          return {"success": True, "content_id": content_id, "status": "published", "published_at": publish_time, "is_scheduled": scheduled_time is not None}
    dependencies: []
    verification_rule: "unapproved_rejected"
    inputs: ["content:Dict[str,Any]", "review_status:str", "scheduled_time:Optional[str]", "required_metadata:Optional[list]"]
    outputs: ["result:Dict[str,Any]"]

  - id: "comment_moderate"
    domain: "media"
    description: "Modera comentarios aplicando reglas de contenido prohibido, spam y filtros de longitud"
    code_block: |
      from typing import Dict, Any, List

      def comment_moderate(comment: Dict[str, Any], blocked_words: List[str] = None, max_length: int = 1000, min_length: int = 1, spam_patterns: List[str] = None) -> Dict[str, Any]:
          """Moderate a comment against blocked words, length rules, and spam patterns."""
          if blocked_words is None:
              blocked_words = []
          if spam_patterns is None:
              spam_patterns = []
          text = comment.get("text", "")
          reasons = []
          if len(text) < min_length:
              reasons.append("too_short")
          if len(text) > max_length:
              reasons.append("too_long")
          text_lower = text.lower()
          found_blocked = [w for w in blocked_words if w.lower() in text_lower]
          if found_blocked:
              reasons.append(f"blocked_word:{found_blocked}")
          found_spam = [p for p in spam_patterns if p.lower() in text_lower]
          if found_spam:
              reasons.append(f"spam_pattern:{found_spam}")
          status = "rejected" if reasons else "approved"
          return {"comment_id": comment.get("id"), "status": status, "reasons": reasons}
    dependencies: []
    verification_rule: "blocked_word_rejected"
    inputs: ["comment:Dict[str,Any]", "blocked_words:List[str]", "max_length:int", "min_length:int", "spam_patterns:List[str]"]
    outputs: ["moderation:Dict[str,Any]"]

  - id: "subscription_manage"
    domain: "media"
    description: "Gestiona suscripciones de contenido: crear, actualizar plan, cancelar y verificar acceso"
    code_block: |
      from datetime import datetime, timedelta, timezone
      from typing import Dict, Any, Optional

      def subscription_manage(action: str, user_id: str, plan: str, subscription_store: Dict[str, Dict[str, Any]], billing_cycle_days: int = 30, new_plan: Optional[str] = None) -> Dict[str, Any]:
          """Manage media subscriptions: create, upgrade/downgrade, cancel, and verify access."""
          sub = subscription_store.get(user_id)
          if action == "create":
              sub = {"user_id": user_id, "plan": plan, "status": "active", "started_at": datetime.now(timezone.utc).isoformat(), "expires_at": (datetime.now(timezone.utc) + timedelta(days=billing_cycle_days)).isoformat()}
              subscription_store[user_id] = sub
              return {"action": "created", "subscription": sub}
          elif action == "update_plan":
              if not sub or sub.get("status") != "active":
                  return {"action": "update_plan", "success": False, "reason": "no_active_subscription"}
              old_plan = sub["plan"]
              sub["plan"] = new_plan or plan
              return {"action": "updated", "old_plan": old_plan, "new_plan": sub["plan"]}
          elif action == "cancel":
              if sub:
                  sub["status"] = "cancelled"
                  sub["cancelled_at"] = datetime.now(timezone.utc).isoformat()
              return {"action": "cancelled", "user_id": user_id}
          return {"action": action, "success": False, "reason": "unknown_action"}
    dependencies: []
    verification_rule: "cancelled_has_no_access"
    inputs: ["action:str", "user_id:str", "plan:str", "subscription_store:Dict[str,Dict[str,Any]]", "billing_cycle_days:int", "new_plan:Optional[str]"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 19. REAL ESTATE (3 modules)
  # ===========================================================================
  - id: "property_list"
    domain: "realestate"
    description: "Lista propiedades inmobiliarias con filtros por precio, ubicación, tipo y características"
    code_block: |
      from typing import Dict, Any, List, Optional

      def property_list(properties: List[Dict[str, Any]], filters: Optional[Dict[str, Any]] = None, sort_by: str = "price", sort_order: str = "asc", limit: int = 20) -> Dict[str, Any]:
          """List properties with filtering by price, location, type, and features."""
          if filters is None:
              filters = {}
          results = properties[:]
          if "min_price" in filters:
              results = [p for p in results if p.get("price", 0) >= filters["min_price"]]
          if "max_price" in filters:
              results = [p for p in results if p.get("price", 0) <= filters["max_price"]]
          if "property_type" in filters:
              results = [p for p in results if p.get("type") == filters["property_type"]]
          if "location" in filters:
              results = [p for p in results if filters["location"].lower() in p.get("location", "").lower()]
          if "min_bedrooms" in filters:
              results = [p for p in results if p.get("bedrooms", 0) >= filters["min_bedrooms"]]
          reverse = sort_order == "desc"
          results.sort(key=lambda p: p.get(sort_by, 0), reverse=reverse)
          return {"total": len(results), "returned": len(results[:limit]), "properties": results[:limit]}
    dependencies: []
    verification_rule: "filters_applied"
    inputs: ["properties:List[Dict[str,Any]]", "filters:Optional[Dict[str,Any]]", "sort_by:str", "sort_order:str", "limit:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "mortgage_calculate"
    domain: "realestate"
    description: "Calcula la cuota mensual de una hipoteca con tasa fija, impuestos y seguro incluidos"
    code_block: |
      from typing import Dict, Any

      def mortgage_calculate(principal: float, annual_rate: float, term_years: int, annual_tax: float = 0.0, annual_insurance: float = 0.0) -> Dict[str, Any]:
          """Calculate monthly mortgage payment including principal, interest, tax, and insurance."""
          monthly_rate = annual_rate / 12
          n_payments = term_years * 12
          if monthly_rate == 0:
              pi_payment = principal / n_payments
          else:
              pi_payment = principal * (monthly_rate * (1 + monthly_rate) ** n_payments) / ((1 + monthly_rate) ** n_payments - 1)
          monthly_tax = annual_tax / 12
          monthly_insurance = annual_insurance / 12
          total_payment = pi_payment + monthly_tax + monthly_insurance
          return {"principal_interest": round(pi_payment, 2), "monthly_tax": round(monthly_tax, 2), "monthly_insurance": round(monthly_insurance, 2), "total_monthly": round(total_payment, 2), "total_paid_over_term": round(total_payment * n_payments, 2), "total_interest": round(pi_payment * n_payments - principal, 2)}
    dependencies: []
    verification_rule: "total_gt_principal_interest"
    inputs: ["principal:float", "annual_rate:float", "term_years:int", "annual_tax:float", "annual_insurance:float"]
    outputs: ["result:Dict[str,Any]"]

  - id: "inspection_schedule"
    domain: "realestate"
    description: "Programa una inspección inmobiliaria verificando disponibilidad del inspector y la propiedad"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, List

      def inspection_schedule(property_id: str, inspector_id: str, requested_time: str, inspector_availability: Dict[str, List[str]], property_availability: Dict[str, List[str]], duration_minutes: int = 60) -> Dict[str, Any]:
          """Schedule a property inspection checking inspector and property availability."""
          date_str = requested_time[:10]
          inspector_busy = inspector_availability.get(inspector_id, [])
          if requested_time in inspector_busy:
              return {"success": False, "reason": "inspector_unavailable"}
          property_busy = property_availability.get(property_id, [])
          if requested_time in property_busy:
              return {"success": False, "reason": "property_unavailable"}
          inspection_id = f"INS-{uuid.uuid4().hex[:8].upper()}"
          return {"success": True, "inspection_id": inspection_id, "property_id": property_id, "inspector_id": inspector_id, "scheduled_time": requested_time, "duration_minutes": duration_minutes, "status": "scheduled", "created_at": datetime.now(timezone.utc).isoformat()}
    dependencies: []
    verification_rule: "conflict_rejected"
    inputs: ["property_id:str", "inspector_id:str", "requested_time:str", "inspector_availability:Dict[str,List[str]]", "property_availability:Dict[str,List[str]]", "duration_minutes:int"]
    outputs: ["result:Dict[str,Any]"]

  # ===========================================================================
  # 20. NONPROFIT (3 modules)
  # ===========================================================================
  - id: "donation_process"
    domain: "nonprofit"
    description: "Procesa una donación registrándola, generando recibo y actualizando métricas de campaña"
    code_block: |
      import uuid
      from datetime import datetime, timezone
      from typing import Dict, Any, Optional

      def donation_process(donor_id: str, amount: float, campaign_id: str, payment_method: str, campaign_store: Dict[str, Dict[str, Any]], is_recurring: bool = False) -> Dict[str, Any]:
          """Process a donation: register it, generate receipt, and update campaign metrics."""
          donation_id = f"DON-{uuid.uuid4().hex[:8].upper()}"
          receipt_number = f"RCPT-{uuid.uuid4().hex[:10].upper()}"
          campaign = campaign_store.get(campaign_id, {})
          current_total = campaign.get("raised", 0.0)
          goal = campaign.get("goal", 0.0)
          campaign_store[campaign_id] = {**campaign, "raised": current_total + amount}
          new_total = current_total + amount
          return {"success": True, "donation_id": donation_id, "donor_id": donor_id, "amount": amount, "campaign_id": campaign_id, "payment_method": payment_method, "is_recurring": is_recurring, "receipt_number": receipt_number, "campaign_progress": round(new_total / goal * 100, 1) if goal > 0 else 0, "donated_at": datetime.now(timezone.utc).isoformat()}
    dependencies: []
    verification_rule: "campaign_total_updated"
    inputs: ["donor_id:str", "amount:float", "campaign_id:str", "payment_method:str", "campaign_store:Dict[str,Dict[str,Any]]", "is_recurring:bool"]
    outputs: ["result:Dict[str,Any]"]

  - id: "volunteer_assign"
    domain: "nonprofit"
    description: "Asigna voluntarios a tareas basándose en habilidades, disponibilidad y necesidad de la tarea"
    code_block: |
      from typing import Dict, Any, List

      def volunteer_assign(volunteers: List[Dict[str, Any]], tasks: List[Dict[str, Any]], max_tasks_per_volunteer: int = 3) -> Dict[str, Any]:
          """Assign volunteers to tasks based on skills matching, availability, and capacity."""
          assignments = []
          volunteer_load = {v["id"]: 0 for v in volunteers}
          for task in sorted(tasks, key=lambda t: t.get("priority", 5)):
              required_skills = set(task.get("required_skills", []))
              best_volunteer = None
              best_match = 0
              for vol in volunteers:
                  if volunteer_load[vol["id"]] >= max_tasks_per_volunteer:
                      continue
                  if task.get("time_slot") and task["time_slot"] not in vol.get("availability", []):
                      continue
                  vol_skills = set(vol.get("skills", []))
                  match_count = len(required_skills & vol_skills)
                  if match_count > best_match:
                      best_match = match_count
                      best_volunteer = vol
              if best_volunteer:
                  assignments.append({"task_id": task["id"], "task_name": task.get("name", ""), "volunteer_id": best_volunteer["id"], "volunteer_name": best_volunteer.get("name", ""), "skill_match": best_match})
                  volunteer_load[best_volunteer["id"]] += 1
          unassigned = [t["id"] for t in tasks if t["id"] not in [a["task_id"] for a in assignments]]
          return {"assigned_count": len(assignments), "unassigned_count": len(unassigned), "assignments": assignments, "unassigned_tasks": unassigned}
    dependencies: []
    verification_rule: "skills_matched"
    inputs: ["volunteers:List[Dict[str,Any]]", "tasks:List[Dict[str,Any]]", "max_tasks_per_volunteer:int"]
    outputs: ["result:Dict[str,Any]"]

  - id: "grant_track"
    domain: "nonprofit"
    description: "Rastrea subvenciones y becas monitoreando plazos, requisitos de informe y estado de desembolso"
    code_block: |
      from datetime import datetime, timezone
      from typing import Dict, Any, List

      def grant_track(grants: List[Dict[str, Any]], reference_date: datetime = None) -> Dict[str, Any]:
          """Track grants monitoring deadlines, reporting requirements, and disbursement status."""
          if reference_date is None:
              reference_date = datetime.now(timezone.utc)
          tracked = []
          total_awarded = 0.0
          total_disbursed = 0.0
          for grant in grants:
              awarded = grant.get("amount_awarded", 0)
              disbursed = grant.get("amount_disbursed", 0)
              total_awarded += awarded
              total_disbursed += disbursed
              deadline = grant.get("report_deadline")
              deadline_status = "on_track"
              if deadline:
                  dl = deadline if isinstance(deadline, datetime) else datetime.fromisoformat(deadline)
                  days_left = (dl - reference_date).days
                  deadline_status = "overdue" if days_left < 0 else "due_soon" if days_left <= 14 else "on_track"
              tracked.append({"grant_id": grant["id"], "funder": grant.get("funder", ""), "awarded": awarded, "disbursed": disbursed, "remaining": awarded - disbursed, "report_deadline_status": deadline_status, "status": grant.get("status", "active")})
          return {"total_grants": len(grants), "total_awarded": total_awarded, "total_disbursed": total_disbursed, "disbursement_rate": round(total_disbursed / total_awarded * 100, 1) if total_awarded > 0 else 0, "grants": tracked}
    dependencies: []
    verification_rule: "overdue_deadline_flagged"
    inputs: ["grants:List[Dict[str,Any]]", "reference_date:datetime"]
    outputs: ["result:Dict[str,Any]"]
