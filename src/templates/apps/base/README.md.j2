# {{ project_name }}

{{ project_name }} — {{ app_name }} application, version {{ version }}.

Auto-generated by [Zenic-Logic](https://github.com/zenic-logic).

---

## Tech Stack

| Layer         | Technology                        |
|---------------|-----------------------------------|
| Framework     | FastAPI 0.110+                    |
| Language      | Python 3.12                       |
| Database      | SQLite (WAL mode)                 |
| ORM           | Raw SQL (parameterized)           |
| Validation    | Pydantic v2                       |
| Templating    | Jinja2                            |
| Server        | Uvicorn                           |
{% if 'jwt_auth' in blocks or 'auth' in blocks %}| Auth          | JWT (python-jose + passlib)       |{% endif %}
{% if 'report' in blocks or 'pdf' in blocks %}| PDF           | WeasyPrint                        |{% endif %}
{% if 'scheduler' in blocks %}| Scheduler     | APScheduler                       |{% endif %}

---

## Quick Start

### 1. Clone & Install

```bash
git clone <repo-url>
cd {{ project_name }}
python -m venv .venv
source .venv/bin/activate   # Linux/macOS
# .venv\Scripts\activate    # Windows
pip install -r requirements.txt
```

### 2. Run the Application

```bash
uvicorn main:app --host 0.0.0.0 --port {{ port }} --reload
```

The app will be available at **http://localhost:{{ port }}**.

### 3. Docker (Alternative)

```bash
docker build -t {{ project_name | lower }} .
docker run -p {{ port }}:{{ port }} {{ project_name | lower }}
```

---

## Project Structure

```
{{ project_name }}/
├── main.py              # FastAPI app, routes, middleware
├── database.py          # SQLite connection & query helpers
├── models.py            # Pydantic schemas
├── services.py          # Business logic layer
├── config.py            # Configuration
├── validators.py        # Input validation & sanitization
├── requirements.txt     # Python dependencies
├── Dockerfile           # Container build
├── templates/           # Jinja2 HTML templates
│   ├── dashboard.html
│   ├── errors/
│   └── {% for entity in entities %}{{ entity }}s/
│       ├── list.html
│       ├── detail.html
│       └── form.html
{% endfor %}└── static/              # CSS, JS, images
```

---

## API Documentation

Once the server is running, interactive docs are available at:

- **Swagger UI**: `http://localhost:{{ port }}/docs`
- **ReDoc**: `http://localhost:{{ port }}/redoc`

### Endpoints Overview

#### System

| Method | Path        | Description          |
|--------|-------------|----------------------|
| GET    | `/health`   | Health check         |
| GET    | `/`         | Dashboard (HTML)     |

{% if 'jwt_auth' in blocks or 'auth' in blocks %}
#### Authentication

| Method | Path                | Description          |
|--------|---------------------|----------------------|
| POST   | `/api/auth/login`   | Login (JWT token)    |
| POST   | `/api/auth/register`| Register new user    |
| GET    | `/api/auth/me`      | Current user profile |

{% endif %}
{% for entity in entities %}
#### {{ entity | capitalize }}

| Method | Path                              | Description                |
|--------|-----------------------------------|----------------------------|
| GET    | `/api/{{ entity }}s`              | List {{ entity }}s (JSON)  |
| GET    | `/api/{{ entity }}s/{id}`         | Get {{ entity }} by ID     |
| POST   | `/api/{{ entity }}s`              | Create {{ entity }}        |
| PUT    | `/api/{{ entity }}s/{id}`         | Update {{ entity }}        |
| DELETE | `/api/{{ entity }}s/{id}`         | Delete {{ entity }}        |
| GET    | `/{{ entity }}s`                  | List page (HTML)           |
| GET    | `/{{ entity }}s/new`              | Create form (HTML)         |
| GET    | `/{{ entity }}s/{id}`             | Detail page (HTML)         |
| GET    | `/{{ entity }}s/{id}/edit`        | Edit form (HTML)           |

{% endfor %}
### Query Parameters (List Endpoints)

| Parameter  | Type   | Default | Description                     |
|------------|--------|---------|---------------------------------|
| `search`   | string | —       | Full-text search across fields  |
| `page`     | int    | 1       | Page number                     |
| `per_page` | int    | 20      | Items per page (max 100)        |

---

## Configuration

All settings can be overridden via environment variables:

| Variable                        | Default                  | Description                    |
|---------------------------------|--------------------------|--------------------------------|
| `APP_ENV`                       | `development`            | `development` or `production`  |
| `DEBUG`                         | `{{ debug }}`            | Enable debug mode              |
| `SECRET_KEY`                    | *(generated)*            | Application secret key         |
| `PORT`                          | `{{ port }}`             | Server port                    |
| `DATABASE_PATH`                 | `./{{ db_name }}.db`     | SQLite database file path      |
| `CORS_ORIGINS`                  | `*`                      | Comma-separated origins        |
{% if 'jwt_auth' in blocks or 'auth' in blocks %}| `JWT_SECRET_KEY`               | *(same as SECRET_KEY)*   | JWT signing key                |
| `JWT_ACCESS_TOKEN_EXPIRE_MINUTES`| `60`                    | Access token lifetime (min)    |{% endif %}

---

## License

Private — all rights reserved.
